<%= form_for(@api_reservation) do |f| %>
  <% if @api_reservation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_reservation.errors.count, "error") %> prohibited this api_reservation from being saved:</h2>

      <ul>
      <% @api_reservation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
