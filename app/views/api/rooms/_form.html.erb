<%= form_for(@api_room) do |f| %>
  <% if @api_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_room.errors.count, "error") %> prohibited this api_room from being saved:</h2>

      <ul>
      <% @api_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
